/**
 * @created     : `!v strftime("%B %d, %Y")`
 * @handle      : @LuchoBazz
 */

#include <bits/stdc++.h>

using namespace std;

void solve_one() {
    ;
}

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    int T = 1;
    cin >> T;
    while (T-- > 0) {
        solve_one();
    }

    return 0;
}
